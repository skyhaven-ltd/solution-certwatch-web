{
  "name": "certwatch-jobs",
  "version": "1.0.0",
  "description": "CertWatch timer-triggered jobs (runs on standalone Function App)",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && func start"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/identity": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
